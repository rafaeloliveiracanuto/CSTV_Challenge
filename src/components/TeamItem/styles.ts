import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    width: '30%',
  },
  text: {
    fontSize: 12,
    color: 'white',
  },
  image: {
    width: 60,
    height: 60,
    resizeMode: 'cover',
    marginBottom: 10,
  },
});
